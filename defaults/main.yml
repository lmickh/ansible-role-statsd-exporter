---
statsd_exporter_args:
  - '-statsd.mapping-config=/tmp/statsd_mapping.conf'
statsd_exporter_image: quay.io/prometheus/statsd-exporter:v0.4.0
statsd_exporter_service_name: "statsd-exporter"

statsd_exporter_mapping:
